# Environment
.env

# Large data files — download separately (Amazon Reviews 2023)
*.jsonl.gz

# Raw data only (too large)
data/raw/

# Generated / processed data — committed to repo for reproducibility
# (individual *.jsonl files are tracked; only raw downloads excluded)

# Trained model weights (too large for GitHub — reproduce via pipeline)
models/embedding/
models/filter_extractor/
# FAISS index + ID map are small — committed
!models/faiss.index
!models/faiss_id_map.json

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
*.egg

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
